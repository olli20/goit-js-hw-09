// файл для Дані

// console.log('1. До виклику setTimeout');

// console.log('2. Лог при виклику колбека через 3 секунди');

// console.log('3. Після виклику setTimeout');

// const logMessage = () => {
//     console.log('2. Лог при виклику колбека через 3 секунди');
// };

// let number = 1;

// if (localStorage.getItem("our-number")) {
//     number = JSON.parse(localStorage.getItem("our-number"));
// };

// setInterval(() => {
    
// console.log(`Я повторилось ${number} раз`);
//     // оновлюємо лічильник
//     number = number + 1;

//     // записуємо в сторедж
//     localStorage.setItem("our-number", JSON.stringify(number));

// }, 1000);


// перевіряємо, яке число зберігається в коробочці number і виводимо правильне речення в консоль

    // if (number === 1) {
    //     console.log(`Я повторилось ${number} раз`);
    // } else if (number === 2 || number === 3 || number === 4) {
    //     console.log(`Я повторилось ${number} рази`);
    // } else if (number > 4) {
    //     console.log(`Я повторилось ${number} разів`);
    // };

